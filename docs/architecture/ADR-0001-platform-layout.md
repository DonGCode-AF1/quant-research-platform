# ADR-0001：可扩展单仓库布局

- 状态：已接受
- 日期：2026-06-22

## 决策

采用单仓库，核心引擎、策略插件、API、研究 UI、实验登记和独立 Quarto 展示共同版本化。此前板块轮动探索不迁移，仅在未来以新实验重新审查。

## 理由

策略只负责目标权重，数据对齐、执行、成本、T+1、绩效和结果格式由核心统一控制，从而减少策略间不可比较的实现差异。临时运行留在 Git 忽略目录，只有审查后结果被提升。

## 后果

平台初期接口较严格；任何特殊执行逻辑需要先扩展核心契约，不能藏在策略插件中。

